server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/springboot_jwt
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false